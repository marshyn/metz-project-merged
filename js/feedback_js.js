// feedback_js.js

var form = document.getElementById('submit'); // get "form" element from HTML

addEvent(form, 'submit', function(e) { 
	e.preventDefault(); // stops from being sent
	var elements = this.elements; // gets all form elements
	var comments = ;
}